<template>
  <nav class="sticky top-0 z-40 bg-white/80 backdrop-blur-xl border-b border-gray-200 shadow-sm dark:bg-gray-900/80 dark:border-gray-800 transition-all duration-300">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex h-16 items-center justify-between">
        <!-- Brand -->
        <div class="flex items-center gap-3 group cursor-pointer">
          <div class="relative">
            <div class="absolute inset-0 bg-brand-500 rounded-full blur-lg opacity-0 group-hover:opacity-30 transition-opacity duration-300"></div>
            <img :src="logoUrl" alt="Logo CellControl" class="h-8 w-auto relative z-10 transition-transform duration-300 group-hover:scale-110" />
          </div>
          <span class="text-gray-900 font-semibold dark:text-gray-100 transition-all duration-300 group-hover:text-brand-600 dark:group-hover:text-brand-400">
            Control de Gestión
          </span>
        </div>

  <!-- Desktop nav -->
  <div class="hidden md:flex items-center gap-1">
          <router-link
            to="/celulares"
            class="px-3 py-2 rounded-md text-sm font-medium transition-colors"
            :class="linkClass('/celulares')"
          >
            <div class="flex items-center gap-2 text-sm">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" />
              </svg>
              Celulares
            </div>
          </router-link>

          <router-link
            to="/usuarios"
            class="px-3 py-2 rounded-md text-sm font-medium transition-colors"
            :class="linkClass('/usuarios')"
          >
            <div class="flex items-center gap-2 text-sm">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" />
              </svg>
              Usuarios
            </div>
          </router-link>

          <router-link
            to="/regiones"
            class="px-3 py-2 rounded-md text-sm font-medium transition-colors"
            :class="linkClass('/regiones')"
          >
            <div class="flex items-center gap-2 text-sm">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7" />
              </svg>
              Regiones
            </div>
          </router-link>

          <router-link
            to="/reparaciones"
            class="px-3 py-2 rounded-md text-sm font-medium transition-colors"
            :class="linkClass('/reparaciones')"
          >
            <div class="flex items-center gap-2 text-sm">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
              Reparaciones
            </div>
          </router-link>

          <!-- Dark mode toggle -->
          <button
            @click="toggleDark()"
            class="ml-2 inline-flex items-center justify-center rounded-md border border-gray-200 px-2.5 py-2 text-sm font-medium text-gray-700 hover:bg-gray-100 dark:border-gray-700 dark:text-gray-300 dark:hover:bg-gray-800"
            :title="isDark ? 'Cambiar a modo claro' : 'Cambiar a modo oscuro'"
          >
            <svg v-if="!isDark" class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v2m0 14v2m9-9h-2M5 12H3m15.364 6.364l-1.414-1.414M7.05 7.05L5.636 5.636m12.728 0l-1.414 1.414M7.05 16.95l-1.414 1.414M12 8a4 4 0 100 8 4 4 0 000-8z" />
            </svg>
            <svg v-else class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z" />
            </svg>
          </button>
        </div>

        <!-- Mobile button -->
        <button 
          @click="mobileOpen = !mobileOpen"
          class="md:hidden inline-flex items-center justify-center p-2 rounded-md text-gray-600 hover:text-gray-900 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-brand-500 dark:text-gray-300 dark:hover:text-gray-100 dark:hover:bg-gray-800"
          aria-label="Toggle menu"
          :aria-expanded="mobileOpen ? 'true' : 'false'"
        >
          <svg v-if="!mobileOpen" class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <svg v-else class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile menu -->
    <div v-show="mobileOpen" class="md:hidden border-t border-gray-200 dark:border-gray-800">
      <div class="px-2 pt-2 pb-3 space-y-1">
        <router-link
          to="/celulares"
          class="block px-3 py-2 rounded-md text-base font-medium"
          :class="linkClass('/celulares')"
          @click="mobileOpen = false"
        >
          Celulares
        </router-link>
        <router-link
          to="/usuarios"
          class="block px-3 py-2 rounded-md text-base font-medium"
          :class="linkClass('/usuarios')"
          @click="mobileOpen = false"
        >
          Usuarios
        </router-link>
        <router-link
          to="/regiones"
          class="block px-3 py-2 rounded-md text-base font-medium"
          :class="linkClass('/regiones')"
          @click="mobileOpen = false"
        >
          Regiones
        </router-link>
        <router-link
          to="/reparaciones"
          class="block px-3 py-2 rounded-md text-base font-medium"
          :class="linkClass('/reparaciones')"
          @click="mobileOpen = false"
        >
          Reparaciones
        </router-link>
      </div>
    </div>
  </nav>
</template>

<script setup>
import { useRoute } from 'vue-router';
import { computed, ref, onMounted } from 'vue';
import logoUrl from '../assets/LogoCellControl.png';

const route = useRoute();
const mobileOpen = ref(false);

const isActive = (path) => computed(() => route.path === path).value;

const linkClass = (path) => {
  if (isDark.value) {
    return isActive(path)
      ? 'text-brand-300 bg-gray-800 border border-gray-700'
      : 'text-gray-300 hover:text-gray-100 hover:bg-gray-800';
  }
  return isActive(path)
    ? 'text-brand-700 bg-brand-50 border border-brand-200'
    : 'text-gray-700 hover:text-gray-900 hover:bg-gray-100';
};

// Dark mode support (Tailwind 'class' strategy)
const isDark = ref(false);
const applyDark = (val) => {
  const root = document.documentElement;
  if (val) root.classList.add('dark');
  else root.classList.remove('dark');
};
const toggleDark = () => {
  isDark.value = !isDark.value;
  localStorage.setItem('prefers_dark', isDark.value ? '1' : '0');
  applyDark(isDark.value);
};
onMounted(() => {
  const stored = localStorage.getItem('prefers_dark');
  if (stored === '1') isDark.value = true;
  // Fallback to OS preference first time
  if (stored === null && window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
    isDark.value = true;
  }
  applyDark(isDark.value);
});
</script>
